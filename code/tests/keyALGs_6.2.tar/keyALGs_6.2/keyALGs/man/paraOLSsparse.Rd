\name{paraOLSsparse}
\alias{paraOLSsparse}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
Parallelized least square fitting for sparse observation matrix.
}
\description{
The speedup comes from multithreading the Gram matrix computation. Multithreading Gauss-Seidel proved no help due to the extremely simple computations eligible for parallelization. Single-threaded Gauss-Seidel already makes solving the Gramian matrix trivial against computing it.
}
\usage{
paraOLSsparse(
  y, X,
  sparseMat = NULL,
  sparseNonZeroThreshold = 0.2,
  maxCore = 7L,
  verboseInt = 8192L,
  useGaussSeidel = TRUE,
  maxIter = 1000000L,
  convergenceEPS = 1e-06)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{y}{
Observed response vector.
}
  \item{X}{
Observation matrix.
}
  \item{sparseMat}{
\code{X}'s representation generated by

\code{keyALGs::fullMatToSparseCol(NumericMatrix Xmat)}.

If \code{sparseMat} already exists, take it in to save computational costs.
}
  \item{sparseNonZeroThreshold}{
If \code{X}'\code{X} has fewer than \code{100 x sparseNonZeroThreshold}\% nonzero entries, \code{X}'\code{X} is seen as a sparse matrix.
}
  \item{maxCore}{
Maximal threads to invoke.
}
  \item{verboseInt}{
Print progress every \code{verboseInt} inner products during computing \code{X}'\code{X}.
}
  \item{useGaussSeidel}{
Use Gauss-Seidel iteration for solving the Gram matrix. If \code{X}'s columns are linearly independent, then \code{X}'\code{X} is positive definite, which guarantees Gauss-Seidel's convergence.
}
  \item{maxIter}{
Maximal iterations for Gauss-Seidel.
}
  \item{convergenceEPS}{
Convergence criterion for Gauss-Seidel.
}
}
\details{
%%  ~~ If necessary, more details than the description above ~~
}
\value{
A list of 3

  coef    : num [1:101, 1] 0.0619 0.0526 ...   = Coefficients.

  pred    : num [1:1000] -0.04374 0.43178 ...  = Model responses.

  totalErr: num 902 = Sum of squared error.
}
\references{
%% ~put references to the literature/web site here ~
}
\author{
%%  ~~who you are~~
}
\note{
%%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
%% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }% use one of  RShowDoc("KEYWORDS")
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
